# 1. Clone your repo (if not already)
git clone https://github.com/mareegpost/xal-forecast.git
cd xal-forecast

# 2. Add the files above
# (Copy-paste app.py, requirements.txt, README.md into folder)

# 3. Push to GitHub
git add .
git commit -m "Initial XAL-FORECAST system"
git push origin main
